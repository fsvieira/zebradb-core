var should = require("should");
var Variable = require("../lib/variable");

describe('Unify() and NotUnify()', function ( ) {
	describe('#No domain, set values', function () {
		it('should', function () {
			
		});
	});

});
