const prepareDefinitions = require("./prepare-definitions");
const checkDefinitions = require("./check-definitions");
const multiplyDefinitions = require("./multiply-definitions");
const prepare = require("./prepare");

module.exports = {
	prepareDefinitions,
	checkDefinitions,
	multiplyDefinitions,
	prepare
};
